# Preserve execution fidelity by default

The default execution profile preserves application behavior and document structure. It does not remove frames, media, or other interactive content to reduce test-run duration.

The explicit `fast` profile can block configured resources or disable selected behaviors. Its test result records the active fidelity policy.
